# 必填：GitHub owner（用户名或组织名，建议小写）
GHCR_OWNER=your-github-owner

# 可选：镜像标签，默认 latest；也可以填具体 SHA 标签
IMAGE_TAG=latest

# 可选：games 环境变量
GAMES_PUBLIC_BASE_URL=https://games.xpsxp.org
GAMES_SMTP_HOST=
GAMES_SMTP_PORT=587
GAMES_SMTP_USER=
GAMES_SMTP_PASS=
GAMES_SMTP_FROM=

# 可选：数据库文件映射路径（宿主机路径）
GAMES_DB_PATH=./volumes/games/data.db
REACTION_DB_PATH=./volumes/reaction/data.db
