services:
  gallery:
    build:
      context: .
      dockerfile: Dockerfile
    image: gallery:local
    container_name: gallery-test
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data/library:/app/data/gallery:ro
      - ./data/state:/app/data/state
